program p
    const int a = 20;
    int b;
    int c[];

    abstract class A {
        abstract void f();
    }

    class B extends A {
        {
            void f() {
                return;
            }
        }
    }

    A objectA;
    B objectB;
{
    void foo(int a) {
    }

    void main() {
        b = 2;
        b--;
        c--;
        c[1]--;
        a--;

        foo(c[1]);
        foo(c);
        foo('a');
        foo(a);
        foo(1);
        foo(true);

        read(1);
        read(a);
        read(b);
        read(c);
        read(c[1]);

        print(1);
        print(a);
        print(b);
        print(c);
        print(c[1]);

        b = 10 < 'a' ? true : 'b';
        b = a <= 0
                ? a > 10
                    ? true
                    : 'a'
                : a;

        b = c.length;

        break;

        if (a == 2) break;

        while(true) {
            break;
        }

        switch (a) {
            case 1: break;
            case 2: {
                a = 2;
            }
        }

        objectA = new A;
        objectA = new B;

        objectB = new A;
        objectB = new B;
    }
}